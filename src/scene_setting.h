#ifndef SCENE_SETTING_H
#define SCENE_SETTING_H

// Designs the SETTING UI,
// and provides a method to render it.
// Function No.4

#include "raylib.h"
#include "button.h"

//struct Button fullscreenButton = createButton(100, 300, 100, 100, "fullScreenButton.png");
//struct Button musicButton = createButton(100, 500, 100, 100, "musicButton.png");

void drawSetting();

#endif